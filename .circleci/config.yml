version: 2.1

jobs:
  test:
    docker:
      - image: circleci/node:latest-browsers
    steps:
      - checkout
      - run: yarn install
      - run:
          name: Run Unit Tests
          command: yarn test

workflows:
  test:
    jobs:
      - test:
          context:
            - main
