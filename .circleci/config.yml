version: 2.1
jobs:
  test:
    executor: equestria/linux
    steps:
      - checkout
      - run: yarn install
      - run:
          name: Run Unit Tests
          command: react-native run-android --variant=debug && yarn test