version: 2.1
jobs:
  test:
    executor: equestria/linux
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm run test